# ch15：本地桌面应用 — Spec

## 背景与目标

为现有 ArtCode 交付一个可在本机打开的单用户桌面应用。优先复用现有 Python 核心，以最小必要改动完成前后端接入。前端布局与样式可独立调整，业务规则继续由原领域模块管理。

## 能力

- 选择已有本地项目及模型配置，打开或恢复会话。
- 在桌面发送任务、制定计划及执行最近计划。
- 实时显示现有模型文字流，显示已完成工具批次的结果。
- 将启动期与执行期审批转换为图形交互。
- 取消当前操作，查看后台任务状态，保留原有命令入口。
- 应用启动后自动管理 Python 后端，退出时清理任务与资源。
- 打包本机可启动应用，日常使用无须分别启动前后端服务。

## 设计骨架

React 展示组件、样式、Zustand 展示状态、桌面通信边界分别组织。Electron 管理窗口与后端进程，Python 桥接器直接调用现有 Application，不创建第二份业务状态。Session 历史仍是持久事实来源。

## 非功能要求

- 保留原 CLI 与领域模块实现，不进行无关重构。
- 前端不能读取模型协议元数据；renderer 不直接访问 Node 或任意 IPC。
- 验证聚焦桥接边界、打包启动与真实模型联调，不跑整仓长测试。
- 可使用现有本地模型配置，不将密钥打入应用或写入前端存储。

## 范围说明

本次用户将目标收敛为最少代码、最少必要测试的个人可运行应用。使用独立的简化桌面协议，不声明实现《前后端接口与执行过程可视化》中完整协议。现有核心公开契约不因桌面适配而改变。

## Out of Scope

完整事件回放与重同步分页、Shell 逐字节实时输出、完整子 Agent 执行树、文件编辑器与 diff、账号与云服务、跨平台安装包、自动更新与签名公证、复杂主题或插件系统。工具批次在现有核心产出结果时显示；任务状态使用只读快照更新。
