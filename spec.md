# ch14：ArtCode 语义统一与破坏性整体重写

## 背景

ArtCode 已通过 ch02–ch10.5、ch12 与 ch13 建立起完整的本地 CLI Coding Agent 能力，但当前实现仍保留明显的章节式增长痕迹：同一业务状态存在多个权威来源，Transcript 与 Prompt 的职责交叠，Tool 能力、权限、Workspace 与隔离策略在多处重复判断，Skill、Subagent、Task 与 Worktree 的生命周期也彼此穿透。这些问题使阅读者必须同时理解大量浅模块、兼容路径和隐式状态，增加了修改、验证和继续学习的成本。

本章进行一次破坏性整体重写。重写必须保留已经确认的现役用户能力及失败语义，但不保留旧命令、旧配置、旧持久格式、旧模块边界或内部接口的兼容性。新实现使用统一领域语言，把外部复杂度封装在少量职责内聚的深模块中，并在完整验收后一次性替换旧核心、删除重复状态与兼容层。

ArtCode 仍是单机、单用户、纯本地的 Python 学习项目。本章不追求生产规模基础设施，也不借重写之名扩展为云端、多用户或跨平台产品。

## 目标用户

- 在已有 Workspace 中使用 ArtCode 完成理解、规划、编码、测试和审查的本地用户。
- 通过 Skill、MCP Server 和 Subagent 扩展本地工作流的进阶用户。
- 希望借 ArtCode 学习 Agent、上下文治理、工具安全、持久化与并发设计的项目维护者。

## 目标

- 用统一领域语言描述 Session、Run、Agent、Tool、Skill 与 Subagent，消除同义概念和语义漂移。
- 让每一份可变状态只有一个权威所有者，并通过不可变快照或明确 Interface 传递给使用方。
- 将 Provider、MCP、文件系统、进程、Seatbelt、Git 和持久格式等外部复杂度隐藏在所属模块内部。
- 保留现役用户能力、失败语义、数据保护与隔离边界，不保留历史实现形状。
- 让普通使用路径保持直接，让复杂能力按需出现，避免用户为内部架构承担额外操作成本。
- 建立可从 Application 入口观察的黑盒验收体系，验证行为而不是旧模块和私有调用顺序。
- 在验收通过后冻结核心领域语言与外部契约，使后续学习型重构主要发生在模块内部。

## 功能需求

### Application 与生命周期

- F1：ArtCode 可以在用户指定的已有 Workspace 中启动，不要求项目采用特定代码结构。
- F2：源码运行与安装后运行进入同一 Application 组装路径，并表现出相同的用户行为。
- F3：Application 是启动、交互、命令分流和关闭的唯一生命周期协调者，不复制其他模块拥有的业务状态。
- F4：用户可以新建 Session、恢复当前 Workspace 最近可写的 Session，或明确选择已有 Session。
- F5：用户级与项目级配置按确定顺序合成，非法配置在产生外部效果前被拒绝，并一次报告同层可发现问题。
- F6：启动、状态、诊断和失败信息不得泄露认证秘密或可由秘密直接推导出的内容。
- F7：启动失败、用户退出、取消和异常关闭都会回收当前进程负责的连接、子进程、后台执行与临时资源，同时保留受保护成果。

### Session、Transcript 与 Prompt

- F8：Session 拥有 Transcript 的提交、持久化和恢复，其他模块不能维护第二份权威对话历史。
- F9：Transcript 只记录已经提交的对话事实，并以追加方式保持原始顺序。
- F10：每次 Run 使用的 Prompt 都是由 Session 基于确定输入生成的不可变投影，不反向成为 Session 历史。
- F11：指令、Notice、Summary、长期记忆、可用 Tool 和 Transcript 以明确边界进入 Prompt，来源之间不得相互伪装。
- F12：Notice 只在下一次适用 Prompt 中投递一次，只读查询不会提前消费它。
- F13：Summary 是可重建的派生表示，生成失败或损坏时不得替换或破坏权威 Transcript。
- F14：Provider 为 Tool 续接要求的 Protocol Metadata 与用户可见内容分离保存，并在适用的后续 Prompt 中完整续接。
- F15：取消、流式中断或请求失败时，已经显示但尚未完成的 Assistant 文本不得提交为 Transcript 事实。

### Agent、Run 与 Model

- F16：Agent 以一个明确目标、Run Mode 和不可变执行上下文推进一次完整 Run，不拥有 Session 持久化、路径安全或 Provider 协议规则。
- F17：普通 Run 持续到模型自然完成、用户取消、请求失败或无法继续，不受产品级固定轮次意外截断。
- F18：自动化和受控执行可以显式施加有限轮次，并以独立 Stop Reason 结束。
- F19：自然完成、达到限制、用户取消、请求失败、长度结束和不可继续等终止原因均可被调用方与用户区分。
- F20：Model 接收完整 Prompt，返回类型化流事件和 Provider 报告的真实 usage，并隐藏底层请求、流协议和连接管理。
- F21：用户可以控制 Thinking 是否参与模型请求，但内部 reasoning 不作为普通文本展示、持久化或写入长期记忆。
- F22：模型最终文本连续流式显示；长度结束和中途失败均保留已经显示的临时文本并明确报告停止状态。
- F23：认证、连接、网络、超时、上下文超限、协议错误与流中断具有可区分的失败类别，且所有错误信息经过脱敏。
- F24：Provider 的单次请求失败不会破坏已提交 Transcript，也不会使后续 Session 无法继续。

### Plan、Act 与 Tool 批次

- F25：Plan Run 只能观察 Workspace 和相关状态，不能通过任何 Tool、规则或批准产生写入与外部效果。
- F26：用户可以审查最近一次成功计划，并以 Act Run 执行该计划。
- F27：执行最近计划时可以补充新约束，新约束与计划共同成为本次 Act Run 的明确输入。
- F28：模型可以在一次响应中提出多个 Tool 请求，Agent 以完整协议边界提交请求与结果。
- F29：相邻且互不依赖的观察型 Tool 可以并发执行，具有变化、外部或控制效果的 Tool 保持模型给出的顺序。
- F30：单个 Tool 的拒绝、非法参数、失败、超时或取消以结构化结果返回 Agent，除非 Run 已不可继续，否则不直接终止整个 Application。
- F31：并发批次中的局部失败不取消无依赖的已开始观察操作，批次结果仍按原 Tool 请求顺序交给 Agent。

### Tool、权限与 Workspace

- F32：Tool 统一提供模型可见能力描述、Tool Effect、Run Mode 适用性、权限要求和结构化结果语义。
- F33：内置 Tool 支持按范围读取文本、查找文件、搜索普通文本、创建文本、明确覆盖、唯一匹配编辑和执行本地命令。
- F34：新文件可以创建 Workspace 内缺失的父目录；覆盖已有内容必须来自明确意图；编辑目标不唯一时不得产生部分修改。
- F35：大文件、长输出和命令结果使用有界展示，并保留可定位的完整结果或明确说明无法完整保留的原因。
- F36：所有文件与命令效果都绑定当前运行上下文中的 Workspace，不能依赖进程级当前目录决定归属。
- F37：路径规范化、Workspace 边界、敏感路径和符号链接检查在授权前与真正执行前都成立，目标在期间变化时拒绝执行。
- F38：命令超时、取消或 Application 关闭会终止并回收该命令的整个进程树。
- F39：权限判断顺序固定为硬约束、显式规则、当前 Run Mode 或 Shell 策略、人工审批，低层决定不能覆盖高层拒绝。
- F40：用户可以对精确目标作出仅本次允许、仅本次拒绝、以后允许和以后拒绝，长期决定不得扩大为模糊授权。
- F41：Plan 只读边界、敏感路径保护和危险命令限制不能被权限规则或人工批准解除。
- F42：Shell 在支持的本地平台上使用强制隔离并阻止网络访问；隔离不可用时失败关闭，脱离隔离必须由用户明确承担风险。
- F43：Tool 执行只读取一份不可变运行上下文来确定 Workspace、Run Mode、权限快照、来源、隔离状态与取消状态。

### MCP

- F44：用户可以配置用户级与项目级 MCP Server，项目级定义按确定规则覆盖同名用户级定义。
- F45：MCP 支持本地进程与远程流式连接方式，并完成初始化、分页发现、会话复用和有序关闭。
- F46：项目级 MCP Server 在产生外部效果前需要确认，每次 MCP Tool 调用都作为潜在副作用单独进入权限流程。
- F47：MCP Tool 目录既可以完整暴露，也可以按需检索激活；已经激活的 Tool 在当前 Session 后续 Run 中保持可用。
- F48：内置 Tool 与 MCP Tool 通过同一 Tool 语义参与 Prompt、策略判断、批次执行和结构化结果回传。
- F49：单个 MCP Server 或 Tool 的配置、连接、协议、超时、取消与关闭失败不得破坏其他 Server、内置 Tool 或主 Run。
- F50：MCP 文本与结构化内容可以进入模型上下文，二进制内容只以受限元数据表示，不被隐式展开进 Prompt。

### 上下文治理、持久化与记忆

- F51：上下文预算优先使用 Provider 返回的真实 usage，并在真实值不可用时采用可解释的确定性估算。
- F52：大型 Tool Result 完整存入当前 Workspace 的内部临时区域，Transcript 与 Prompt 只保留可定位预览。
- F53：Agent 可以对已存盘结果进行受范围限制的分段回读，回读不得隐式把完整原文重新注入上下文。
- F54：上下文接近窗口边界时可以压缩较早的 Assistant、Tool 与内部 System 历史，但全部 User 原文始终逐条、逐字、按顺序保留。
- F55：压缩不得破坏 Assistant Tool Request 与 Tool Result 的协议边界，也不得把 Protocol Metadata 改写为自然语言事实。
- F56：自动、强制、紧急与手动压缩均有界且可观察；压缩失败保留旧历史，并通过有限重试或明确停止避免循环。
- F57：项目级与用户级持久指令按确定优先级加载，引用内容受到来源、路径、循环和规模边界约束。
- F58：每条已提交 Transcript 事实同步追加到人类可检查的本地存档，进程异常不能留下被当作完整事实的半条记录。
- F59：Session 恢复可以隔离独立坏记录、修复不完整尾部，并在 Tool 协议不完整时回退到最近安全前缀。
- F60：不同进程不能同时写入同一 Session；只读检查和失败的恢复尝试不能改变原存档。
- F61：恢复的 Session 保留最近有效计划，并在长间隔后通过一次性 Notice 提醒用户重新核对外部环境。
- F62：长期记忆区分用户级偏好与项目级事实，以人类可编辑内容和可重建索引保存。
- F63：只有自然完成的 Run 才能基于其不可变快照异步更新长期记忆，取消、失败与达到限制的 Run 不进入记忆。
- F64：长期记忆更新串行执行且故障隔离，失败不得改变当前回复、Transcript 或下一次 Run。

### 本地命令与 TUI

- F65：以命令标记开头的输入确定地进入本地命令分流，未知或非法命令不得回退给模型或写入 Transcript。
- F66：除规划、执行计划和压缩等明确需要模型的流程外，本地状态与控制命令不调用 Model。
- F67：用户可以查看和控制帮助、退出、Plan/Act、压缩、权限、Shell 隔离、Session、记忆、状态、Task 与受保护 Worktree。
- F68：视觉清屏只清理终端显示并结束当前 Skill 激活，不删除 Transcript、Session、计划、记忆或 usage 状态。
- F69：TUI 支持多行输入、流式文本、运行取消、人工审批、精简进度与 usage 展示，以及前台和后台 Task 控制。
- F70：所有状态视图以脱敏、只读快照为基础，查看状态不得消费 Notice、修改权限或改变执行顺序。

### Skill

- F71：Skill 可以从项目、用户、内置和已提供的扩展来源发现，同名定义按确定优先级整体覆盖。
- F72：无效的高优先级 Skill 不得静默回退到同名低优先级版本，其他有效 Skill 仍可继续使用。
- F73：启动时只把 Skill 的选择信息加入可用目录，实际使用时才加载完整 SOP 与能力声明。
- F74：Skill 可以通过自然语言选择或明确命令激活，并在当前 Session 中持续生效直到被清除或替换。
- F75：多个已激活 Skill 对普通 Tool 的允许范围取交集，且任何 Skill 只能收窄而不能绕过 Run Mode、权限、路径、危险命令、隔离或 MCP 确认。
- F76：Skill 文件热更新对新执行生效；更新无效时保留当前执行已经冻结的快照并提供诊断。
- F77：共享 Skill 在主 Run 与主 Transcript 中执行；Isolated Skill 使用临时 Transcript，只向主 Run 返回最终总结。
- F78：Isolated Skill 不创建 Task、不进入后台队列、不获得 Worktree，也不成为 Subagent。
- F79：Skill 指定的模型选择只影响该次适用执行，目标模型不可用时明确失败，不静默切换。

### Subagent、Task 与 Worktree

- F80：主 Agent 通过一个稳定入口创建定义式或 Fork 式 Subagent，两种形式共享 Task 调度与执行语义。
- F81：定义式 Subagent 从干净 Transcript 和指定 Role 开始；Fork 式 Subagent 使用创建时冻结的父 Prompt 与 Tool 快照。
- F82：Role 从项目、用户、内置和已提供的扩展来源确定性覆盖，无效高优先级同名 Role 不得静默回退。
- F83：每个 Subagent 独立维护消息、权限事件、文件缓存、上下文状态、轮次和 usage，并复用同一套 Agent、Model 与 Tool Interface。
- F84：Subagent 的有效能力只能由父能力上限、Role 和后台策略继续收窄，不能复用父 Run 的临时批准。
- F85：Subagent 以非交互方式运行，不能再次委派、询问用户或自行绕过需要人工确认的操作。
- F86：Task 支持排队、受控并发、前台等待后转后台、列表、详情、取消与完成顺序通知。
- F87：Task 通知只在父 Run 的安全 Prompt 边界投递一次，并只回传结论、状态、usage 与成果交接信息。
- F88：Task 只在当前进程中存在；进程重启不恢复执行，但会重新识别并保护可能包含成果的遗留 Worktree。
- F89：可能写盘的 Subagent 在开始执行前绑定独立 Worktree，其创建基准在任务入队时冻结，并且不复制主 Workspace 的未提交内容。
- F90：主 Workspace、各 Subagent Worktree 与系统托管元数据彼此隔离，任一 Agent 都不能越权读写其他作用域。
- F91：没有文件变化和新增提交的系统临时 Worktree 可以安全清理；包含未提交修改或未安全交接提交的 Worktree 必须保留并可定位。
- F92：ArtCode 不自动合并、变基、推送或丢弃 Subagent 成果，危险丢弃只能由用户对精确目标再次确认。

### 重写切换与契约冻结

- F93：新核心收敛为 Application、Session、Agent、Model、Tool、Workspace、Skill 与 Subagent 八个职责模块，目录数量不构成约束。
- F94：每个可变状态只有一个权威所有者，跨模块协作使用明确 Interface、不可变快照或显式交接数据。
- F95：新实现独立完成并通过完整验收后一次性替换旧核心，不长期维持新旧双轨。
- F96：切换时删除旧核心、重复状态、章节式产品文本和兼容层，不为旧内部调用方保留浅转发模块。
- F97：现役章节的用户能力与失败语义必须保持，已经退役的评测系统不进入新核心。
- F98：最终验收前允许一次集中调整公开契约；验收通过后，领域语言与外部契约默认冻结，后续优化优先发生在模块内部。

## 非功能要求

- N1 语义一致性：实现、用户文档、测试和后续规格统一使用 `CONTEXT.md` 中的领域术语，同一术语不得承担多个互斥含义。
- N2 单一权威：权限、Transcript、Skill 激活、Task、usage、Workspace 与其他可变状态均只有一个可定位的权威来源。
- N3 模块深度：调用方只需理解稳定 Interface，不得被迫了解 Provider 协议、持久格式、MCP 会话、路径校验或 Git 生命周期的内部细节。
- N4 依赖方向：外部 Adapter 依赖所属模块的 Interface，核心业务模块不得反向依赖 CLI/TUI、具体 Provider、具体持久格式或测试替身。
- N5 数据保护：取消、错误、异常退出、恢复、清理和并发执行不得静默丢失已提交事实或未安全交接的文件成果。
- N6 失败隔离：单次请求、单个 Tool、MCP Server、Skill、Subagent、记忆更新或通知失败不得无关地破坏其他运行单元。
- N7 确定性：相同输入、冻结快照和外部响应产生相同的策略、Prompt、执行顺序、恢复结果与 Stop Reason。
- N8 可观测性：每个 Run 与 Task 的模式、状态、Stop Reason、usage、权限决定和成果位置都可通过用户界面或公开 Interface 观察。
- N9 安全边界：路径、权限、危险命令、沙箱与成果保护必须由确定性代码执行，不能仅依赖提示词约束。
- N10 秘密与内部数据：认证秘密、内部 reasoning、完整大型结果和 Subagent 过程轨迹不得意外进入终端、Prompt、Transcript 或长期记忆。
- N11 资源治理：连接、文件句柄、子进程、后台执行、锁和临时目录都有明确所有者，并在所有终止路径中得到处理。
- N12 上下文效率：未使用的 Skill、完整大型结果、过早的 Subagent 过程和可重建派生数据不得无条件占用 Prompt。
- N13 可测试性：各深模块可通过公开 Interface 使用确定性替身测试，最高层行为可从 Application 入口进行黑盒验证。
- N14 本地优先：设计服务于单机、单用户学习场景，不为假设的远程规模预建通用平台层。
- N15 简洁性：只有在减少生产代码、状态数量和认知成本时才引入成熟库或额外私有 seam，不为形式完整增加层次。
- N16 兼容原则：重写只承诺现役行为等价，不承诺旧命令、配置、文件格式、模块、类、函数或测试形状兼容。

## 设计骨架

系统按以下职责边界组织；模块是语义边界，不要求一一对应顶层目录：

- Application：唯一的依赖组装与生命周期入口，协调启动、Session 选择、用户交互、本地命令和关闭。
- Session：拥有 Transcript 的提交、持久化与恢复，并把 Transcript、指令、Notice、Summary、长期记忆和 Tool 能力投影为不可变 Prompt。
- Agent：接收目标、Run Mode 与不可变执行上下文，推进 Model 与 Tool 循环，产出结构化运行事件和 Stop Reason。
- Model：隐藏 Provider 请求、流协议、Thinking、Protocol Metadata、usage、连接复用、错误映射和脱敏。
- Tool：统一内置与 MCP 能力的描述、Tool Effect、批次计划、权限决策、人工审批、执行与结构化结果。
- Workspace：拥有文件、进程、路径边界、Shell 隔离、内部临时结果和 Worktree 等全部本地效果。
- Skill：负责发现、解析、覆盖、激活、热更新和执行快照，并向 Session、Tool 与 Agent 提供 SOP、能力收窄和模型选择。
- Subagent：负责 Role、子 Run、Task 调度、后台状态、通知和成果交接，并通过 Workspace 获得隔离工作副本。

核心协作关系如下：

- Application 创建并持有模块生命周期，但业务状态留在各自所有者中。
- Session 在 Run 开始前生成不可变 Prompt 与执行快照，Agent 不直接修改 Transcript。
- Agent 只通过 Model Interface 获取流事件，只通过 Tool Interface 请求效果；完成后由 Session 决定哪些事实可以提交。
- Tool 根据不可变运行上下文统一规划和执行内置 Tool 与 MCP Adapter，并把全部本地效果委托给 Workspace。
- Skill 通过投影和能力收窄参与主 Run，不直接拥有主 Transcript 或绕过 Tool 策略。
- Subagent 通过同一 Agent、Model 与 Tool Interface 创建独立 Run；Task 归属和 Worktree 生命周期通过显式交接数据协作。

不建立横跨所有模块的通用 Event Bus、Repository 层、依赖注入容器、插件框架或多 Provider 产品抽象。模块内部可以使用少量私有 seam，但不得让调用方承担被封装的复杂度。

## Out of Scope

- 不恢复、重建或继续扩展 ch11/chTA 的评测系统、排行榜、报告、基准与简历指标管线。
- 不删除或弱化已经确认的现役用户能力与失败语义。
- 不建设云端多用户服务、分布式调度、常驻后台平台、团队协作或生产规模治理。
- 不增加多个原生 Provider 协议、运行时 Provider 切换或未经项目所有者确认的新模型产品能力。
- 不支持 Linux、Windows、容器或虚拟机上的 Shell 沙箱。
- 不实现网络域名白名单、动态网络授权、代理平台或交互式网络放行。
- 不建设 Skill 市场、联网安装、版本解析、依赖管理或远程共享。
- 不实现 Subagent 多层递归、群聊、投票、长期队列恢复或自动团队编排。
- 不自动合并、变基、推送、创建 Pull Request 或替用户处理 Git 冲突。
- 不引入数据库、向量检索、Embedding、云同步、跨机器记忆或本地秘密加密。
- 不建设全屏 TUI、Web 界面或桌面图形界面。
- 不为旧命令、旧配置、旧持久格式、旧模块、旧内部接口或旧测试夹具保留兼容层。
- 不处理与本次整体重写无关的安全强化、性能优化和新产品功能。

## 完成标准

- AC1：从源码入口与安装入口分别在已有 Workspace 启动，执行相同交互、Session 选择和退出路径，观察到等价结果且所有已创建资源被回收。（覆盖 F1–F7）
- AC2：连续完成多个 Run 后检查持久数据与下一次 Prompt，Transcript 只含已提交事实，Prompt、Notice、Summary 与 Protocol Metadata 各自保持定义边界。（覆盖 F8–F15）
- AC3：分别构造自然完成、显式限制、取消、长度结束、Provider 失败和不可继续场景，用户可区分 Stop Reason，未完成文本不污染 Transcript。（覆盖 F16–F24）
- AC4：完成一次只读 Plan、带补充约束的 Act 和含多个 Tool 的批次，确认 Plan 无副作用、观察型调用可以并发、效果型调用保持顺序且局部失败结构化返回。（覆盖 F25–F31）
- AC5：从 Application 入口完成读取、查找、搜索、创建、覆盖、唯一编辑和命令执行，验证结果有界、目标明确、失败不留部分写入。（覆盖 F32–F35）
- AC6：针对相对路径、绝对路径、父级跳转、敏感路径、符号链接和审批后目标变化执行黑盒测试，任何效果都不能逃离当前 Workspace。（覆盖 F36–F37、F43）
- AC7：分别触发命令完成、超时、取消和关闭，确认整个进程树被回收；对四类用户权限决定和各层冲突逐项验证，硬约束始终优先。（覆盖 F38–F41）
- AC8：在支持的平台验证 Shell 强制隔离与网络阻断，在隔离不可用和用户明确脱离隔离两条路径中均得到可观察且不静默降级的结果。（覆盖 F42）
- AC9：使用真实本地进程与远程流式 MCP Server 完成初始化、分页发现、完整暴露、按需激活、调用和关闭，内置 Tool 与 MCP Tool 通过同一策略执行。（覆盖 F44–F48）
- AC10：分别注入单个 MCP Server 的配置、连接、协议、超时、取消和关闭故障，其他 Server、内置 Tool 与主 Run 仍可继续，二进制结果不进入 Prompt 正文。（覆盖 F49–F50）
- AC11：构造真实 usage、usage 缺失、大型 Tool Result 和分段回读场景，确认预算来源可解释、完整结果可定位且 Prompt 只承载受控内容。（覆盖 F51–F53）
- AC12：在长 Session 中反复触发各类压缩，逐字比较所有 User 消息并检查 Tool 协议；失败后旧历史可继续使用，且不会进入无界重试。（覆盖 F54–F56）
- AC13：验证多来源指令、同步追加、坏记录、坏尾部、不完整 Tool 协议、并发锁、计划恢复和长间隔 Notice，任何恢复路径都不伪造或静默删除已提交事实。（覆盖 F57–F61）
- AC14：分别完成自然结束、取消、失败和达到限制的 Run，只有自然结束快照进入对应作用域的长期记忆；记忆故障不影响当前与后续 Run。（覆盖 F62–F64）
- AC15：逐项操作本地命令与 TUI，确认未知命令不调用 Model、不写 Transcript，状态查询只读且脱敏，清屏仅影响显示和 Skill 激活。（覆盖 F65–F70）
- AC16：验证多来源 Skill 覆盖、坏候选、两阶段加载、自然语言与命令激活、白名单交集、热更新和模型不可用，任何路径都不能扩大 Tool 能力。（覆盖 F71–F76、F79）
- AC17：分别运行共享 Skill 与 Isolated Skill，确认前者使用主 Transcript，后者仅返回最终总结且不产生 Task、后台执行或 Worktree。（覆盖 F77–F78）
- AC18：启动定义式与 Fork 式 Subagent，检查上下文来源、Role 覆盖、状态隔离、能力收窄和非交互限制，子执行复用相同核心 Interface 而不复制业务语义。（覆盖 F80–F85）
- AC19：以受控并发运行多个 Task，完成前台转后台、查看、取消和逆序完成，确认每个 Task 只在安全边界通知一次且只回传精简交接信息。（覆盖 F86–F88）
- AC20：在主 Workspace 存在未提交修改时并行启动两个写任务，确认它们基于冻结提交进入不同 Worktree，三方文件互不覆盖且越权访问被拒绝。（覆盖 F89–F90）
- AC21：分别构造无变化、未提交修改、本地新增提交和用户确认危险丢弃场景，只有安全目标被自动清理，ArtCode 生命周期内不出现自动合并、变基或推送。（覆盖 F91–F92）
- AC22：通过依赖审计证明八个职责模块各有单一状态所有者，外部 Adapter 不向核心泄漏协议与存储细节，调用方不依赖私有 seam。（覆盖 F93–F94）
- AC23：新核心通过全部受保护行为矩阵后一次性切换，旧核心、重复状态、章节式产品文本和兼容层从生产导入路径消失，退役评测系统未被重新引入。（覆盖 F95–F97）
- AC24：全部功能需求均映射到自动黑盒测试或明确人工验收；非 live、真实 Provider、真实 MCP、真实进程、真实隔离、构建与安装验证均通过或被明确记录为环境阻塞。（覆盖 F1–F98、N1–N16）
- AC25：最终代码、用户文档、`CONTEXT.md` 与公开契约一致，并记录重写验收完成及核心冻结决策。（覆盖 F98）
