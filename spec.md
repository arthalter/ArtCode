# ch02：让AI说话

## 背景

ArtCode 是一个使用 Python 开发的本地 CLI Coding Agent 学习项目。本章的核心任务不是让 ArtCode 读取文件、编辑代码或执行工具，而是先打通一个 Coding Agent 最基础的能力：用户能在终端里和 AI 进行连续、流式、多轮对话。

这一章要让 ArtCode 从“只是一个项目名”变成“能在命令行里开口说话的助手”。后续的工具调用、代码编辑、项目理解、权限控制和会话记忆，都建立在本章的对话循环、Provider 抽象、流式输出和上下文管理之上。

## 目标用户

- 正在从零学习 CLI Coding Agent 架构的项目作者。
- 希望在本地终端中使用 DeepSeek API 进行流式对话的用户。
- 希望理解模型流式响应、对话上下文、Provider 抽象和 TUI 输入输出关系的新手开发者。

## 本章目标

用户启动 ArtCode 后，进入一个增强型命令行对话界面。用户可以输入问题，ArtCode 调用 DeepSeek 的 OpenAI-compatible API，将模型最终回答以连续流式方式显示在终端里，并在本次运行期间保留对话上下文，使 AI 能理解前文。

本章完成后，ArtCode 应该能稳定支持“启动、输入、流式回复、多轮追问、取消生成、退出”的完整纯对话闭环。

## 能力清单

- 启动后进入增强型 CLI 对话界面。
- 从项目根目录读取单一 YAML 配置文件。
- 使用 DeepSeek API，并通过 OpenAI-compatible 协议接入。
- 使用自行处理的 SSE 流式响应，而不是等待完整回复后再输出。
- 支持 Thinking Mode，并通过配置控制是否启用和使用何种思考强度。
- 不显示模型 thinking/reasoning 过程，只显示最终回答。
- 使用 Conversation Context 保存本次运行内的对话历史。
- 支持多轮对话，后续问题可以引用前文。
- 支持基础 Slash Commands，用于帮助和退出。
- Slash Commands 的设计要允许后续扩展更多本地命令。
- 支持多行输入，适合粘贴长问题、代码片段和报错信息。
- 支持生成期间取消当前回复。
- 对常见配置错误、认证错误、网络错误、模型错误、超时和流式中断给出友好提示。
- 支持开发调试方式启动，也支持安装后的命令入口启动。
- 提供真实 DeepSeek API 集成测试。

## 配置边界

本章只支持项目根目录中的单一配置文件，不支持多 profile，不支持运行时切换 Provider。

配置应表达四类核心信息：

- 使用哪种协议。
- 使用哪个模型。
- 请求哪个 API 根地址。
- 使用什么认证密钥。

真实配置文件包含敏感信息，应被 Git 忽略。项目应提供一个不包含真实密钥的示例配置文件，方便用户复制后填写。

Thinking Mode 属于配置的可选扩展能力。本章只要求在 DeepSeek/OpenAI-compatible 场景下生效，不做模型能力自动探测，也不做完整 Provider 能力协商。

## 设计骨架

ArtCode 本章采用分层设计，目录结构可以为后续章节预留空间，但每层只实现 ch02 必须能力。

- 入口层：负责启动 ArtCode。
- 配置层：负责读取、校验和脱敏展示配置。
- Runtime 层：负责串联配置、Provider、上下文、命令系统和 TUI。
- Conversation 层：负责维护本次运行内的对话上下文。
- Commands 层：负责处理基础斜杠命令，并为后续命令扩展留位置。
- Providers 层：负责模型 API 调用、SSE 解析、Thinking Mode 映射和错误转换。
- TUI 层：负责用户输入、状态显示、角色标签、流式输出和取消行为。

## 对话上下文

本章引入 Conversation Context 作为对话历史的抽象概念。

ch02 只要求它保存本次进程运行期间的对话消息。程序退出后不要求恢复历史，也不要求保存会话文件。设计上应避免把上下文能力写死为不可替换的裸列表，为后续扩展 Session Store、Context Window Manager、项目上下文注入和长期记忆预留空间。

本章不做自动上下文裁剪。如果对话过长导致模型请求失败，应作为普通 API 错误提示给用户。

## 流式输出

ArtCode 应在收到模型增量内容后立即输出，不等待完整回复生成完毕。

本章不承诺严格逐字输出，而是要求连续流式输出。实际显示粒度由 API 返回的增量内容决定。

模型 reasoning/thinking 内容即使在响应中存在，也不应展示给用户，不应写入日志，也不应保存为会话内容。

## TUI 体验

TUI 应比普通 REPL 更舒适，但不做全屏应用。

启动时应展示完整的非敏感状态信息，让用户确认当前协议、模型、请求地址、流式状态和 Thinking Mode 状态。每轮输入提示符应保持简洁，不重复刷屏。

AI 回复内容按模型返回的文本原样流式输出。角色标签、状态栏、错误提示和命令帮助可以使用颜色增强，但本章不做 Markdown 重渲染、代码块高亮或复杂排版。

## 错误处理

配置类错误在启动阶段提示并退出。对话请求类错误应在 TUI 中显示，并让程序回到可继续输入的状态。

错误提示应面向新手，帮助用户理解问题大概出在哪里。任何错误展示都不得泄露完整认证密钥。

本章不要求自动重试、错误日志文件或完整错误恢复机制。

## 非功能要求

- 不泄露完整 API key。
- 不生成日志文件。
- 不保存完整对话记录。
- 不保存 reasoning/thinking 内容。
- 不做会话恢复。
- 不做自动上下文裁剪。
- 不做自动重试。
- 不使用模型 SDK 封装流式调用。
- 不让 TUI 直接依赖 DeepSeek 原始响应结构。
- 不让半截取消回复污染 Conversation Context。

## 完成标准

- 用户可以启动 ArtCode 并进入增强型命令行聊天界面。
- 用户可以输入问题并看到连续流式回复。
- 用户可以进行多轮追问，AI 能引用前文。
- 用户可以通过基础斜杠命令查看帮助和退出。
- 用户可以在生成期间取消当前回复，取消内容不会进入上下文。
- ArtCode 能通过真实 DeepSeek API 完成端到端对话验证。
- 配置、上下文、命令、Provider、SSE 和 TUI 的边界清楚，后续能继续扩展 agent 能力。

## Out of Scope

- tool use。
- 文件读取。
- 文件编辑。
- shell 命令执行。
- MCP。
- Hook。
- Skill 系统。
- 权限系统。
- 沙箱。
- 人工确认流程。
- 会话持久化。
- 长期记忆。
- 自动上下文压缩。
- 多 Provider profile。
- 运行时切换 Provider。
- Markdown 重渲染。
- 代码块语法高亮。
- 展示 thinking/reasoning 过程。
