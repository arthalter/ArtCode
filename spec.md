# ch13：子 Agent 与 Worktree 隔离

## 背景

ArtCode 当前以单个交互式 Agent 为核心。复杂任务产生的文件内容、工具结果、编译错误和修复过程会持续进入主对话，使后续子任务受到无关信息干扰，带来上下文膨胀、Token 浪费和响应质量下降。

当任务进入并行阶段时，仅隔离消息还不够。后台子 Agent 与主 Agent 如果共享同一个工作目录，可能同时读取或修改相同文件，造成中间状态泄漏、修改互相覆盖以及构建结果污染。Git 分支只能保存不同版本，不能让多个版本在同一时间拥有独立工作目录。

本章将子 Agent 委派和 Git Worktree 文件隔离作为一个完整能力建设：主 Agent 把适合独立处理的任务交给子 Agent；子 Agent 在隔离的运行时中非交互执行；可能并行写文件的任务从启动时就在独立 Worktree 中工作；任务完成后只把成果、状态和文件交接信息返回主对话。

## 目标用户

- 使用 ArtCode 在本地仓库中完成分析、编码、测试和审查任务的用户。
- 负责理解全局目标、拆分子任务并调度子 Agent 的主 Agent。
- 通过角色文件为项目定义专用子 Agent 的项目维护者。

## 目标

- 让主 Agent 可以通过一个稳定入口委派独立子任务。
- 让固定职责的专家从干净上下文开始，避免继承无关对话。
- 让临时助手继承父对话快照，减少背景复述并利用提示缓存。
- 隔离每个子 Agent 的运行状态，同时复用无须隔离的基础设施。
- 让子 Agent 能非交互运行到底，并在前台、后台之间安全切换。
- 让后台任务可追踪、可取消、可查看结果与资源用量。
- 让并行写任务使用独立 Worktree，避免不同 Agent 互相覆盖文件。
- 在任何自动退出或清理路径中保护未提交和未安全交接的成果。
- 只向主对话回传有用成果，避免重新引入子 Agent 的过程噪声。

## 功能需求

### 子 Agent 创建与角色

- F1：系统提供一个结构稳定的 Agent 工具，通过明确的创建类型分流定义式与 Fork 式两条路径。
- F2：定义式子 Agent 必须选择预定义角色，并从不包含父对话历史的独立对话启动。
- F3：Fork 式子 Agent 继承调用瞬间的父对话和工具快照，并可选择叠加一个预定义角色。
- F4：Fork 叠加角色时，角色只能改变身份、模型选择和能力限制，不能扩大父 Agent 原有权限。
- F5：角色使用带 YAML frontmatter 的 Markdown 定义，元信息描述身份、用途、工具范围、模型档位、轮次限制、权限模式和隔离需求，正文作为贯穿子 Agent 生命周期的系统提示。
- F6：角色可来自项目、用户、内置和插件来源，同名角色按项目、用户、内置、插件的顺序整体覆盖，不进行跨来源字段合并。
- F7：无效的高优先级角色定义会阻止同名低优先级角色被静默启用，同时不影响其他有效角色使用。
- F8：角色目录在启动子 Agent 前刷新，使后续调用可以使用最新的有效定义，已经运行的任务继续使用启动时快照。
- F9：逻辑模型档位通过配置映射到实际模型，未配置的档位明确不可用，继承模式沿用父 Agent 模型。

### 运行时隔离与执行

- F10：每个子 Agent 独立维护消息、权限事件、文件读取缓存、上下文管理状态、轮次和 Token 用量。
- F11：子 Agent 共享模型访问基础设施、Hook 引擎、工具实现和底层文件系统服务。
- F12：子 Agent 使用独立权限追踪，不继承父 Agent 的临时批准，并受父权限上限、角色限制和全局规则共同约束。
- F13：子 Agent 采用非交互的运行到底模式，模型停止调用工具时结束任务并形成最终结果。
- F14：普通工具失败会返回子 Agent 供其调整，达到轮次限制或发生不可恢复错误时会保留已有成果并以异常状态结束。
- F15：定义式子 Agent 可在前台运行，也可显式、自动或由用户手动切换到后台；Fork 式子 Agent 始终在后台运行。
- F16：前台转后台不会重启子 Agent、切换工作目录或丢失已经产生的消息、文件和用量状态。

### 后台任务与结果回传

- F17：后台任务管理器追踪排队、运行、完成、异常、达到限制和取消等状态。
- F18：后台启动会立即返回任务标识，使主 Agent 可以继续处理当前对话或委派其他任务。
- F19：用户和主 Agent 都能查看任务列表、任务详情、最终结果、权限事件、轮次、耗时和 Token 用量，并能取消尚未结束的任务。
- F20：后台结果按完成顺序异步通知主对话，不打断父 Agent 正在进行的模型调用。
- F21：主对话只接收子 Agent 的最终结论、任务状态、资源用量和文件交接信息，不注入完整子对话与工具轨迹。
- F22：超过通知承载范围的结果保存在任务详情中，主对话接收可识别任务与成果位置的摘要。
- F23：取消任务不会回滚已经完成的文件操作，可能包含成果的 Worktree 继续受保留规则保护。
- F24：应用正常退出时会暴露仍在运行的后台任务，避免在用户不知情的情况下结束任务并丢失成果。

### 工具与权限防线

- F25：系统通过全局禁止规则、角色限制、后台允许范围和运行时来源检查共同计算子 Agent 的有效能力。
- F26：定义式子 Agent 不获得再次委派子 Agent 或向用户发起交互询问的能力。
- F27：Fork 式子 Agent 即使继承了父工具定义，也不能再次 Fork、创建其他子 Agent 或向用户发起交互询问。
- F28：子 Agent 中任何仍需临时用户确认的操作都会被自动拒绝，并将拒绝原因返回该子 Agent 和记录到任务详情。
- F29：角色白名单不能恢复全局或后台禁止的工具，黑名单与更高层限制发生冲突时以限制更严的一方为准。

### Worktree 隔离与生命周期

- F30：可能写文件或执行写盘命令的子 Agent 必须在启动时绑定独立 Worktree，完全只读的子 Agent 可以不创建 Worktree。
- F31：定义式角色声明写能力时必须同时声明 Worktree 隔离；无角色 Fork 继承写能力时由系统自动启用 Worktree。
- F32：新 Worktree 基于任务启动或入队时记录的当前提交创建，不复制主工作区的未提交修改。
- F33：需要 Worktree 的任务在当前目录不是可用 Git 仓库或不存在可用提交时明确失败，不自动初始化仓库或替用户提交。
- F34：Worktree 使用仓库内不被版本控制追踪的托管目录，并为临时子 Agent 目录和分支使用可识别的系统命名。
- F35：Worktree 名称经过字符、长度、分段和最终路径边界校验，拒绝路径遍历、绝对路径和逃离托管目录的结果。
- F36：目标目录已存在时，系统通过只读文件检查验证其仓库、分支和任务归属，匹配后快速恢复，不匹配时拒绝复用。
- F37：新 Worktree 创建后补齐项目声明的本地配置、Git Hook、共享依赖和被忽略但运行所需的文件。
- F38：共享依赖采用尽力而为的复用策略，解析到主工作区的共享目标不允许被子 Agent 写入。
- F39：系统不改变进程级工作目录，每次工具调用显式携带所属会话的工作目录。
- F40：文件内容、系统提示、项目指令和记忆等路径相关缓存均以绝对路径区分不同工作目录，进入和退出 Worktree 时不依赖全量清缓存。
- F41：Worktree 子 Agent 不能写入主工作区、其他子 Agent Worktree 或托管边界之外的项目路径。
- F42：子 Agent 完成后，未产生文件变化和新增提交的临时 Worktree 自动清理；存在未提交修改或未安全交接提交的 Worktree 保留。
- F43：用户手动创建的 Worktree 不参与子 Agent 自动清理，删除时继续受到变更保护。
- F44：危险删除必须由用户显式确认丢弃成果，模型不能自行绕过保护。
- F45：后台清理只处理可验证为系统临时目录、没有活跃使用、已经过期且 Git 状态安全的 Worktree。
- F46：应用异常退出后不恢复后台任务，但会在后续运行中识别遗留 Worktree，并继续保护可能包含成果的目录。

### Git 成果交接

- F47：子 Agent 结果包含创建基准、工作分支、Worktree 是否保留、目录位置、变更状态和提交交接状态。
- F48：子 Agent 可以在权限允许时创建本地提交，向远端推送仍需同时满足任务意图和既有权限约束。
- F49：系统不自动把子 Agent 分支合并回主分支，由主 Agent 或用户在任务结束后决定如何审查和合并。

## 非功能要求

- N1 安全性：任何来自模型的目录名、分支名、工具选择和删除意图都必须经过独立验证，不能仅依赖提示词约束。
- N2 数据保护：默认行为不得删除未提交修改、未跟踪文件或尚未安全交接的提交。
- N3 隔离性：一个 Agent 的消息、权限事件、文件缓存和用量不得写入另一个 Agent 的运行时状态。
- N4 并发性：多个后台任务并行运行时，不得依赖进程级工作目录等全局可变状态决定文件操作位置。
- N5 确定性：任务排队后仍使用入队时记录的对话、权限和代码基准，不因实际启动时间变化而漂移。
- N6 可观测性：每个任务均可定位其状态、来源、角色、运行方式、停止原因、用量和文件成果。
- N7 失败隔离：单个子 Agent、Worktree 初始化或后台通知失败不得破坏主 Agent 对话和其他运行中的任务。
- N8 缓存效率：Fork 首次请求尽可能保持父请求前缀稳定，并仅依据模型服务返回的数据报告缓存效果。
- N9 性能：已有 Worktree 的恢复优先使用有限的只读文件检查，避免无必要的 Git 子进程和依赖目录复制。
- N10 兼容性：不需要 Worktree 的现有单 Agent 工作流保持可用，现有工具实现通过显式工作目录适配新的运行环境。
- N11 可测试性：角色覆盖、权限收紧、后台切换、路径校验、变更保护和异步通知均能在不依赖人工观察内部代码的情况下验证。
- N12 本地优先：所有任务、角色和 Worktree 能力服务于本地学习型项目，不以远程团队服务或长期后台平台为前提。

## 设计骨架

系统由以下职责边界组成：

- Agent 工具入口：接收创建类型、任务和可选角色，完成参数分流，并返回前台结果或后台任务标识。
- 角色目录：发现、解析、校验和覆盖多来源 Markdown 角色，输出单一有效角色快照与诊断。
- 子 Agent 运行时工厂：为每个任务组装独立对话、权限追踪、缓存、用量和上下文管理状态，同时注入共享基础设施。
- 能力过滤器：按照全局、创建类型、角色、后台状态和调用来源计算最终可执行能力。
- 运行到底执行器：驱动子 Agent 连续执行模型与工具轮次，直到自然完成、取消、达到限制或出现不可恢复错误。
- 后台任务管理器：管理并发、排队、状态、取消、结果、用量和通知投递。
- Worktree 管理器：负责安全命名、创建、快速恢复、初始化、状态检测、退出、保留、删除和过期清理。
- 工作目录解析器：让所有路径相关操作从当前 Agent 会话取得显式工作目录，并以绝对路径隔离缓存。
- 结果通知器：在父 Agent 的安全轮次边界写入精简结果，并保留可查看的完整任务详情。

定义式和 Fork 式共享同一运行到底执行器与后台任务管理器，但使用不同的对话来源和工具约束。Worktree 在子 Agent 开始执行前完成绑定，前后台切换只改变等待方式，不改变该 Agent 的文件环境。

## Out of Scope

- 不实现 Worktree 分支到主分支的自动合并、变基或冲突解决策略。
- 不同步主工作区未提交修改到新 Worktree。
- 不在 Worktree 之间实时同步代码或构建产物。
- 不实现后台任务、子 Agent 对话或任务队列的跨会话持久化与恢复。
- 不实现多个平等 Agent 之间的群聊、投票、共享待办或长期团队编排。
- 不实现子 Agent 的多层递归委派。
- 不自动创建远端分支、Pull Request 或替用户决定是否推送成果。
- 不保证所有依赖目录都适合软链接；不兼容的项目由配置选择独立安装。
- 不把子 Agent 的完整执行轨迹注入主对话。

## 完成标准

- AC1：主 Agent 使用同一个 Agent 工具可以分别启动定义式任务和 Fork 式任务，工具结构在两种调用之间保持不变。
- AC2：定义式任务看不到父对话内容，Fork 式任务能使用启动时的父对话内容，两个子 Agent 的后续消息互不影响。
- AC3：Fork 叠加角色后仍无法获得父 Agent 没有的工具或更高权限。
- AC4：同名多来源角色按既定优先级产生唯一结果，无效高优先级定义不会静默回退。
- AC5：两个并行子 Agent 的消息、权限事件、缓存和 Token 用量分别统计，主 Agent 状态不被覆盖。
- AC6：子 Agent 在模型不再调用工具时自然结束，并向主 Agent 返回最终结果而非完整执行历史。
- AC7：定义式任务可以从前台进入后台且不中断执行，Fork 式任务启动后立即以后台任务形式可见。
- AC8：后台任务可以被列出、查看和取消，完成通知不会插入父 Agent 正在生成的模型请求中间。
- AC9：子 Agent 尝试再次创建子 Agent、询问用户或执行需要临时审批的操作时被阻止，并留下可查看原因。
- AC10：两个并行写任务分别在不同 Worktree 中修改同名文件，主工作区及彼此的文件内容不被直接改变。
- AC11：主工作区存在未提交修改时，新 Worktree 仍以记录的提交为基准，未提交内容不会被隐式复制。
- AC12：恶意或非法 Worktree 名称不能在托管目录之外创建文件或分支。
- AC13：已有合法 Worktree 可以通过只读检查恢复，归属不匹配的已有目录不会被覆盖或接管。
- AC14：Worktree 中的工具调用使用该目录作为工作位置，同时主 Agent 和其他子 Agent 继续使用各自目录。
- AC15：无变更临时 Worktree 在任务完成后可自动清理，包含未提交修改或未安全交接提交的 Worktree 会保留并在结果中标明。
- AC16：过期清理不会删除手动 Worktree、活跃 Worktree 或包含未保护成果的 Worktree。
- AC17：端到端运行一次并行编码子任务后，主 Agent 能看到任务结论、用量、变更状态、Worktree 路径和分支交接信息，且主工作区文件未被子 Agent 直接修改。
