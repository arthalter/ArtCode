# ArtCode

ArtCode is a local Python CLI coding agent learning project.

## ch02: 让AI说话

This chapter connects a terminal chat loop to DeepSeek's OpenAI-compatible Chat Completions API.

Live integration tests read `artcode.yaml` from the project root, call the real DeepSeek API, require network access, and consume DeepSeek API quota.
