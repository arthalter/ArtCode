# Global Notes

## 语言偏好

- 默认使用简体中文回复用户；除非用户明确要求其他语言。


## 项目代码修改协作规则

- 当需要修改项目中的代码内容时（仅限代码），必须先说明修改计划，并按 `1、2、3、4` 列出方案或步骤，由用户选择或明确批准后，才能改动代码。
- 完成任务后，如果用户命令 push 到 GitHub，提交或 PR 备注必须包含完成任务的时间、任务序号、任务功能。
- 用户没有允许修改项目代码时，不得改动代码。（仅限于代码）
- 用户明确要求进行代码改动时，不要做保守改动，应大胆、完整地推进实现。
- 尽量为每一个任务补充单元测试；无法补充时，至少执行对应验证并说明原因。
- 只要存在可运行的真实 API / live integration test，就默认执行真实测试；不要因为 DeepSeek API 额度消耗而跳过，DeepSeek API 成本可忽略。

## 项目说明
我正在构建[一个Cli coding agent项目]，项目名叫[ArtCode]，使用的编程语言是[python]。

每次我会提出一个初步的想法，需要你通过向我提问，帮助我澄清需求、挖掘边缘场景。澄清清楚后共创三份文档保存到项目根目录：

提问时，应当尽可能的详细，一个一个提出你能想到的几乎所有问题。由于用户是新手，很多东西完全没入门，所以需要你在提问时给予一定的解释和引导，并在我要求你详细阐述时为我客观严谨的讲清楚利弊（基于我的这个项目是不打算上线不打算上传，纯本地的学习型项目）

我才用的模式，每一个模块都需要你重新生成文档，所以我需要你在生成文档时，文档名和大标题为ch0x：xxxx的格式，例如ch02:让AI开口说话。这方面的信息我会在提问时给你。


# 三份文档的角色与边界

## spec.md
回答：要解决什么问题、做哪些能力、不做哪些、什么算完成。
写：背景、目标用户、能力清单（一句话一条）、非功能要求、设计骨架、Out of Scope
不写：具体函数名 / 参数名 / 默认值 / 错误文本 / 行号 / SDK 类型名

## tasks.md
回答：按什么顺序做、每步动什么文件。
- 5~15 个任务，每个能在一次专注会话内完成
- 每个任务标注：影响文件、依赖任务、参考资料定位
- 最后一定有「接入主流程」+「端到端验证」两个任务

## checklist.md
每一项必须可勾选、可观测，不许写「实现完整」「质量良好」。
- 把 spec 里被砍掉的具体值放进来作为验收项
- 至少一条端到端验收
